# default.custom.yaml
# save it to: 
#   ~/.config/ibus/rime  (linux)
#   ~/Library/Rime       (macos)
#   %APPDATA%\Rime       (windows)

patch:
  ascii_composer/switch_key:
    Shift_L: commit_code
  schema_list:
    - schema: luna_pinyin
    - schema: easy_en
